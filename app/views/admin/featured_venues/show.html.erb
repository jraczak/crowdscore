<%= render partial: 'admin/subnav' %>

<h1><%= link_to resource.name, Venue.find(resource.venue_id) %></h1>

<h3><%= resource.active %></h3>
<h3><%= "#{resource.city}, #{resource.state}" %></h3>

<h3>Marketing Description</h3>
<p><%= resource.description %></p><br>

<%= link_to "Edit featured venue", edit_admin_featured_venue_path(resource) %>